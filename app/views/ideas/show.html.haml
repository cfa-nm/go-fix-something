= render idea

%ul.no-bullet
  = render partial: 'comments/comment',
           collection: idea.comments,
           layout: 'shared/list_item'
  %li= render 'comments/form'

= link_to 'Edit', edit_idea_path(@idea)
\|
= link_to 'Back', ideas_path
